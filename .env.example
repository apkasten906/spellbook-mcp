# Example environment variables for Spellbook MCP
# Copy this to ".env" and edit values for local development if needed.
# See docs/LOGGING.md for logging-related variables and notes.

# Logging
# Directory that will contain mcp-<DATE>.log, mcp-error-<DATE>.log and cucumber-verbose.log
LOG_DIR=./mcp-starter/logs

# Enable verbose MCP logging (mirrors selected output to console and enables test verbose logs)
# Set to 1 or true to enable
LOG_MCP=0

# Rotation settings (accepted by winston-daily-rotate-file as human-readable sizes)
# Examples: 1m, 5m, 10m
LOG_ROTATE_MAX_BYTES=1m
# Number of rotated files to keep
LOG_ROTATE_BACKUPS=3

# Optional: override the MCP server log file (not normally required)
# LOG_MCP_LOG_PATH=./mcp-starter/logs/mcp.log

# Optional: override the cucumber verbose test log path
# LOG_TEST_VERBOSE_PATH=./mcp-starter/logs/cucumber-verbose.log

# Runtime / test harness
# If you need to override the command used to start the MCP server in tests
# e.g. MCP_CMD=node
MCP_CMD=node
# Arguments used when starting the MCP server for tests (JSON array string)
# e.g. MCP_ARGS=["mcp-starter/server.js"]
MCP_ARGS=["mcp-starter/server.js"]

# Notes:
# - Copy this file to .env for local development (do NOT commit .env).
# - The repo uses winston + winston-daily-rotate-file; see docs/LOGGING.md for details on log files and rotation.
# - To enable verbose acceptance tests in PowerShell for a single session:
#   $env:LOG_MCP = '1'; npm run acceptance:logging
